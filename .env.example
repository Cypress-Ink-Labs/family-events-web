# GitHub Packages (read:packages PAT needed for `pnpm install` to fetch @cypress-ink-labs/* packages)
NODE_AUTH_TOKEN=ghp_your-pat-here

# Web app environment variables — copy apps/web/.env.example to apps/web/.env for local dev.
# All VITE_* variables and other app vars are documented there; do not duplicate them here.
# See apps/web/.env.example for the full list.
